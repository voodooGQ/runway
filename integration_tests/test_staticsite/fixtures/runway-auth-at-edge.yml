deployments:
  - modules:
    - path: auth-at-edge
      type: static
      environments:
        dev:
          namespace: dev
          staticsite_auth_at_edge: true
          staticsite_create_user_pool: true
      options:
        build_output: build
    regions:
      - us-east-1
