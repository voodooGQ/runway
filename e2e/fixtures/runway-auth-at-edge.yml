deployments:
  - modules:
    - path: e2e-auth-at-edge
      type: static
      environments:
        dev:
          namespace: dev
          staticsite_auth_at_edge: true
      options:
        build_output: build
    regions:
      - us-east-1
